#include <bits/stdc++.h>
using namespace std;

int main()
{

	int t;
	cin >> t;
	while (t--)
	{
		//participant, interval , duration

		int participant, interval, duration;
		cin >> participant >> interval >> duration;
		int ans = 0;
		while (duration > ans)
		{
			ans += interval;


		}
		if

		cout << ans << endl;
	}
}
